通过 `images` 数组传入参考图，支持以下形式：

| 输入形式 | 写法 |
|----------|------|
| 公网 URL | 可直接访问的图片地址 |
| Base64 | 图片文件编码后的完整 Base64 字符串 |
| data URL | `data:image/png;base64,<完整图片编码>` |

公网图片必须能够被服务端访问，否则可能出现图片下载错误。将参考图和修改要求一起提交，例如“保持构图，改为水彩风格”。
