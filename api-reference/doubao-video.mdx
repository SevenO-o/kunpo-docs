---
title: "视频生成（Seedance）"
description: "基于 Seedance 2.0 的视频生成 API，支持文生视频、图生视频、视频续写等多种模式。"
---

<span id="豆包视频生成" />

豆包视频生成采用异步任务模式：先提交任务获取 `task_id`，再轮询查询结果。

## 接口地址

```
POST https://llm.ziy.cc/v1/video/generations       # 提交任务
GET  https://llm.ziy.cc/v1/video/generations/{task_id}  # 查询结果
```

---

## 可用模型

| 模型名 | 说明 |
|--------|------|
| `doubao-seedance-2-0-260128` | 高品质模式，画质更佳 |
| `doubao-seedance-2-0-fast-260128` | 快速模式，出图更快 |

---

## 文生视频

<CodeGroup>
  ```bash cURL
  curl -X POST https://llm.ziy.cc/v1/video/generations \
    -H "Authorization: Bearer sk-你的密钥" \
    -H "Content-Type: application/json" \
    -d '{
      "model": "doubao-seedance-2-0-260128",
      "prompt": "一只柴犬在樱花树下奔跑，慢动作，电影感",
      "metadata": {
        "ratio": "16:9",
        "duration": 5,
        "watermark": false,
        "generate_audio": true
      }
    }'
  ```

  ```python Python
  import requests

  resp = requests.post(
      "https://llm.ziy.cc/v1/video/generations",
      headers={"Authorization": "Bearer sk-你的密钥"},
      json={
          "model": "doubao-seedance-2-0-260128",
          "prompt": "一只柴犬在樱花树下奔跑，慢动作，电影感",
          "metadata": {
              "ratio": "16:9",
              "duration": 5,
              "watermark": False,
              "generate_audio": True
          }
      }
  )

  task_id = resp.json()["task_id"]
  print(f"任务已提交: {task_id}")
  ```

  ```javascript Node.js
  const resp = await fetch("https://llm.ziy.cc/v1/video/generations", {
    method: "POST",
    headers: {
      Authorization: "Bearer sk-你的密钥",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      model: "doubao-seedance-2-0-260128",
      prompt: "一只柴犬在樱花树下奔跑，慢动作，电影感",
      metadata: {
        ratio: "16:9",
        duration: 5,
        watermark: false,
        generate_audio: true,
      },
    }),
  });

  const { task_id } = await resp.json();
  console.log(`任务已提交: ${task_id}`);
  ```
</CodeGroup>

### 提交任务响应

```json
{
  "id": "task_xxxxxxxxxxxx",
  "task_id": "task_xxxxxxxxxxxx",
  "model": "doubao-seedance-2",
  "status": "queued",
  "created_at": 1715689600
}
```

用 `task_id` 去查询进度。提交响应的 `status` 固定为小写 `queued`，与轮询返回的大写枚举不是同一套取值。

---

## 图生视频 / 多模态生成

通过 `metadata.content` 传入图片、视频、音频参考，支持以下模式：

- **图生视频**：以参考图为首帧/参考风格生成视频
- **视频续写**：以参考视频为基础续写
- **首尾帧控制**：指定视频的起始帧和结束帧

<AccordionGroup>
  <Accordion title="图生视频">
    ```bash
    curl -X POST https://llm.ziy.cc/v1/video/generations \
      -H "Authorization: Bearer sk-你的密钥" \
      -H "Content-Type: application/json" \
      -d '{
        "model": "doubao-seedance-2-0-260128",
        "prompt": "placeholder",
        "metadata": {
          "content": [
            {
              "type": "text",
              "text": "镜头缓慢推进，云雾在山间飘动，日出时分"
            },
            {
              "type": "image_url",
              "image_url": {"url": "https://example.com/mountain.jpg"},
              "role": "reference_image"
            }
          ],
          "ratio": "16:9",
          "duration": 8,
          "watermark": false,
          "generate_audio": true
        }
      }'
    ```
  </Accordion>

  <Accordion title="首尾帧控制">
    ```bash
    curl -X POST https://llm.ziy.cc/v1/video/generations \
      -H "Authorization: Bearer sk-你的密钥" \
      -H "Content-Type: application/json" \
      -d '{
        "model": "doubao-seedance-2-0-260128",
        "prompt": "placeholder",
        "metadata": {
          "content": [
            {
              "type": "image_url",
              "image_url": {"url": "https://example.com/start.jpg"},
              "role": "first_frame"
            },
            {
              "type": "image_url",
              "image_url": {"url": "https://example.com/end.jpg"},
              "role": "last_frame"
            }
          ],
          "ratio": "16:9",
          "duration": 5
        }
      }'
    ```
  </Accordion>

  <Accordion title="Python 完整示例">
    ```python
    import time
    import requests

    BASE_URL = "https://llm.ziy.cc"
    HEADERS = {"Authorization": "Bearer sk-你的密钥"}

    def submit_video(prompt, image_url=None, ratio="16:9", duration=5):
        """提交视频生成任务，返回 task_id。"""
        metadata = {
            "ratio": ratio,
            "duration": duration,
            "watermark": False,
            "generate_audio": True,
        }

        if image_url:
            metadata["content"] = [
                {"type": "text", "text": prompt},
                {
                    "type": "image_url",
                    "image_url": {"url": image_url},
                    "role": "reference_image",
                },
            ]
            prompt = "placeholder"

        resp = requests.post(
            f"{BASE_URL}/v1/video/generations",
            headers=HEADERS,
            json={
                "model": "doubao-seedance-2-0-260128",
                "prompt": prompt,
                "metadata": metadata,
            },
        )
        resp.raise_for_status()
        return resp.json()["task_id"]


    def poll_video(task_id, interval=5, timeout=300):
        """轮询任务直到完成，返回视频 URL。"""
        start = time.time()
        while time.time() - start < timeout:
            resp = requests.get(
                f"{BASE_URL}/v1/video/generations/{task_id}", headers=HEADERS
            )
            data = resp.json()["data"]
            status = data["status"]

            if status == "SUCCESS":
                return data["result_url"]
            elif status == "FAILURE":
                raise Exception(f"视频生成失败: {data.get('fail_reason')}")

            print(f"状态: {status}，等待 {interval}s...")
            time.sleep(interval)

        raise TimeoutError("轮询超时")


    # 使用示例
    task_id = submit_video(
        prompt="镜头缓慢推进，云雾在山间飘动",
        image_url="https://example.com/mountain.jpg",
        duration=8,
    )
    video_url = poll_video(task_id)
    print(f"视频地址: {video_url}")
    ```
  </Accordion>
</AccordionGroup>

---

## 查询任务结果

<CodeGroup>
  ```bash cURL
  curl https://llm.ziy.cc/v1/video/generations/{task_id} \
    -H "Authorization: Bearer sk-你的密钥"
  ```

  ```python Python
  import requests

  resp = requests.get(
      f"https://llm.ziy.cc/v1/video/generations/{task_id}",
      headers={"Authorization": "Bearer sk-你的密钥"}
  )
  print(resp.json())
  ```
</CodeGroup>

### 响应示例

```json
{
  "code": "success",
  "message": "",
  "data": {
    "task_id": "task_xxxxxxxxxxxx",
    "status": "SUCCESS",
    "progress": "100%",
    "action": "image_to_video",
    "result_url": "https://ark-acg-cn-beijing.tos-cn-beijing.volces.com/...mp4?X-Tos-Expires=86400&...",
    "fail_reason": ""
  }
}
```

| 字段 | 说明 |
|------|------|
| `data.status` | 任务状态（见下表） |
| `data.progress` | 进度百分比字符串，如 `"100%"` |
| `data.result_url` | 视频地址（仅 `SUCCESS` 时可用） |
| `data.fail_reason` | 失败原因（仅 `FAILURE` 时有值） |

<Warning>
  字段都在 **`data`** 对象里，视频地址是 **`data.result_url`**。

  豆包视频的 `result_url` 是**火山引擎 TOS 的预签名链接**（带 `X-Tos-Expires`，通常 24 小时后失效），
  与图片生成不同，**不会转存到 KUNPO CDN**。请在拿到后尽快下载或转存到自己的存储。
</Warning>

### 任务状态

轮询返回的 `data.status` 是大写枚举，与提交响应的小写 `queued` 不是同一套取值：

| 状态 | 说明 |
|------|------|
| `NOT_START` | 已受理入库，尚未开始生成 |
| `QUEUED` | 上游排队中 |
| `IN_PROGRESS` | 生成中，可读 `data.progress` |
| `SUCCESS` | 生成完成，可获取 `data.result_url` |
| `FAILURE` | 生成失败，查看 `data.fail_reason` |

<Note>
  只把 `SUCCESS` 和 `FAILURE` 当作终态；遇到未列出的取值一律按「继续等待」处理。
</Note>

<Tip>
  建议每 5 秒轮询一次，视频生成通常需要 30~120 秒。
</Tip>

---

## 请求参数说明

<ParamField path="model" type="string" required>
  模型名称：`doubao-seedance-2-0-260128`（高品质）或 `doubao-seedance-2-0-fast-260128`（快速）。
</ParamField>

<ParamField path="prompt" type="string" required>
  视频描述文本。使用 `metadata.content` 多模态时可填 `"placeholder"`。
</ParamField>

<ParamField path="metadata" type="object" required>
  视频生成控制参数。
</ParamField>

<Accordion title="`metadata` 子参数">

<ParamField path="metadata.ratio" type="string">
  视频宽高比：`21:9` / `16:9` / `4:3` / `1:1` / `3:4` / `9:16` / `adaptive`。
</ParamField>

<ParamField path="metadata.duration" type="integer">
  视频时长（秒），范围 4~15。
</ParamField>

<ParamField path="metadata.watermark" type="boolean" default="false">
  是否添加水印。
</ParamField>

<ParamField path="metadata.generate_audio" type="boolean" default="true">
  是否生成背景音频。
</ParamField>

<ParamField path="metadata.tools" type="array">
  启用联网搜索增强（仅纯文本输入时有效）：`[{"type": "web_search"}]`。
</ParamField>

<ParamField path="metadata.content" type="array">
  多模态输入数组，包含 `text` / `image_url` / `video_url` / `audio_url` 元素，每个元素可附加 `role` 字段。

  `role` 可用值：`reference_image` / `reference_video` / `reference_audio` / `first_frame` / `last_frame`。
</ParamField>

</Accordion>
