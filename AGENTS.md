# KUNPO API 文档协作约定

编辑本仓库前读取 `DOCS_MAINTENANCE.md`；结构变化同时维护 `PROJECT_CONTEXT.md`。发布流程见 `DEPLOYMENT.md`。

用户的长期要求：**这里的 API 文档主要给 AI 阅读，用于准确生成接入代码。** 先保证协议精确、示例可执行、状态与约束明确，再考虑展示文案。不要以营销介绍替代接口契约，也不要因“给 AI 看”省略鉴权、默认值、单位、错误和恢复路径。

模型参数、计费矩阵、协议差异在对应模型页保持单一信息源，导航和简介只链接。必须区分 KUNPO 网关行为、供应商行为和未开放能力，不可按供应商原文推定 KUNPO 已支持。

禁止在公开文档、示例或仓库中写真实密钥、用户子应用 ID、私人素材地址。多人协作时保留其他人的修改。部署成功前不要将待发布记录改为已上线。
